# Versions used to produce the submitted result, recorded from the training environment
# (Python 3.12.13; pip freeze). Later versions of most of these are fine, but torch must be at
# least 2.7 with CUDA 12.8 for the RTX 5090 (sm_120) used for training, and numpy 2.4.6 requires
# Python 3.11 or newer. Optional, not needed for the scored recipe: pyctcdecode and kenlm (only
# for the unused beam/LM decode path), fast_mp3_augment (only for MP3 augmentation, which was
# absent in the training environment and is skipped with a printed notice).
torch==2.11.0
transformers==4.57.6
accelerate==1.14.0
datasets==3.6.0
jiwer==4.0.0
rapidfuzz==3.14.5
soundfile==0.14.0
librosa==0.11.0
numpy==2.4.6
pandas==3.0.3
pyyaml==6.0.3
huggingface-hub==0.36.2
audiomentations==0.43.1
omegaconf==2.3.1
